// // utils/moodleHelpers.js
// export function getMoodleImageUrl(fileurl) {
//   const baseUrl = import.meta.env.VITE_MOODLE_BASE_URL;
//   const token = import.meta.env.VITE_MOODLE_TOKEN;

//   if (!fileurl) return '';
//   if (!fileurl.startsWith('/')) fileurl = '/' + fileurl;

//   return `${baseUrl}${fileurl}?token=${token}`;
// }
